/**
 * create-client.ts
 *
 * Future script to scaffold a new client folder under vault/clients/[slug]/
 * by copying vault/clients/_template/ and pre-filling the slug.
 *
 * Usage: npx tsx scripts/create-client.ts --slug acme-tours --name "Acme Tours"
 */

console.log("Client folder scaffolding placeholder");
