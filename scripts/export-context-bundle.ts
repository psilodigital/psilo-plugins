/**
 * export-context-bundle.ts
 *
 * Future script to assemble a portable context bundle for a given task
 * (e.g. all relevant agent, skill, vault, and workflow files concatenated
 * into a single markdown bundle for an external agent runtime).
 *
 * Usage: npx tsx scripts/export-context-bundle.ts --workflow client-meeting-prep --client artway
 */

console.log("Context bundle export placeholder");
