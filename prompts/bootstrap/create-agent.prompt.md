# Create Agent Prompt

Create a new agent folder under `agents/[agent-name]`.

The agent folder must include:
- agent.md

The agent.md must include:
- role
- responsibilities
- allowed skills
- allowed vault folders
- restricted actions
- approval required for
- output standards

The agent must not contain full procedures. Procedures live in skills.

Match the simple style of existing agents.
