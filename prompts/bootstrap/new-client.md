# Bootstrap: New Client

Use this prompt to start a brand new client engagement from scratch.

---

You are operating inside the Psilodigital Business Agent System. Read `SYSTEM.md` and `AGENTS.md` first if you haven't already.

We are onboarding a new client. Here's what I have:

- **Client name:** <fill in>
- **Source:** <referral / inbound / outbound>
- **Initial context:** <one paragraph from email, intro call, or notes>
- **Next event:** <e.g. discovery call on YYYY-MM-DD>

Please:

1. Create the client folder under `vault/clients/<slug>/`.
2. Run the `client-research` skill and produce `research.md`.
3. If a meeting is on the calendar, run the `client-meeting-prep` workflow.
4. Surface anything ambiguous as a question for me to resolve before we go further.

Output a one-screen summary at the end with links to the created files.
